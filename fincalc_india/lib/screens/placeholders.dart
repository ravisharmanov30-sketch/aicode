import 'package:flutter/material.dart';

class FDRDCalculator extends StatelessWidget {
  const FDRDCalculator({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('FD/RD Calculator')), body: const Center(child: Text('Coming Soon')));
  }
}

class EMICalculator extends StatelessWidget {
  const EMICalculator({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('EMI Calculator')), body: const Center(child: Text('Coming Soon')));
  }
}

class CapitalGainsCalculator extends StatelessWidget {
  const CapitalGainsCalculator({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Capital Gains')), body: const Center(child: Text('Coming Soon')));
  }
}

class RetirementCalculator extends StatelessWidget {
  const RetirementCalculator({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('PF/NPS Calculator')), body: const Center(child: Text('Coming Soon')));
  }
}
